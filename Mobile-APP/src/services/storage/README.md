# Storage Shared Services

Cross-feature storage utilities (path policies, retention rules, cleanup jobs) belong here.

_Currently empty._ Feature-local storage stays with its owning feature — e.g.
[capture/services/captureStorage.ts](../../features/capture/services/captureStorage.ts)
and the file helpers in [export/sessionPackager.ts](../../features/export/sessionPackager.ts).
