# Services Layer

Cross-feature service modules belong here once they are shared by at least two features.

Feature-local services should remain inside the owning feature folder until reuse is proven.
